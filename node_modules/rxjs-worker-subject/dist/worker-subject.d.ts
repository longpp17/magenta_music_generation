import { Subject } from 'rxjs';
export declare class WorkerSubject<Input, Output> extends Subject<Output> {
    worker: Worker;
    constructor(worker: Worker, isRawResponse?: boolean);
    next<Input>(input: Input): void;
    complete(terminate?: boolean): void;
}
